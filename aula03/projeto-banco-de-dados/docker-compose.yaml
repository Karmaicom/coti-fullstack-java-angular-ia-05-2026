version: '3.8'

services:
  postgres: #Nome do serviço
    image: postgres:16 #Instalador do serviço -> Busca a ultima versao do PostgreSQL
    container_name: postgres_bd_clientes #Nome do container (VM)
    restart: always #Sempre inicializar o serviço
    environment:
      POSTGRES_DB: bd-clientes
      POSTGRES_USER: coti
      POSTGRES_PASSWORD: Coti@2026
    ports:
      - "5433:5432" #Portas  --> 5433 no Windows : 5432 no Docker
    volumes:
      - postgres_data:/var/lib/postgresql/data #Pastas
    networks:
      - clientes_network #Rede compartilhada

  pgadmin:
    image: dpage/pgadmin4:latest #Nome do serviço
    container_name: pgadmin_bd_clientes #Nome do container (VM)
    restart: always #Sempre inicializar o serviço
    environment:
      PGADMIN_DEFAULT_EMAIL: admin@admin.com
      PGADMIN_DEFAULT_PASSWORD: Coti@2026
    ports:
      - "5050:80" #Porta
    depends_on:
      - postgres
    networks:
      - clientes_network

volumes:
  postgres_data:

networks:
  clientes_network:
    driver: bridge