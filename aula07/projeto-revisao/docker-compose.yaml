version: '3.8'

services:
  postgres:
    image: postgres:16
    container_name: postgres_projeto_revisao
    restart: always
    environment:
      POSTGRES_DB: bd-projeto-revisao
      POSTGRES_USER: coti
      POSTGRES_PASSWORD: Coti@2026
    ports:
      - "5433:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    networks:
      - projeto_revisao_network

  pgadmin:
    image: dpage/pgadmin4:latest
    container_name: pgadmin_projeto_revisao
    restart: always
    environment:
      PGADMIN_DEFAULT_EMAIL: admin@admin.com
      PGADMIN_DEFAULT_PASSWORD: Coti@2026
    ports:
      - "5050:80"
    depends_on:
      - postgres
    networks:
      - projeto_revisao_network

volumes:
  postgres_data:

networks:
  projeto_revisao_network:
    driver: bridge